# v0 baseline (rank.py + src/scorer.py) uses ONLY the Python standard library.
# Pin exact versions once Phase 2 lands. Suggested Phase 2 deps (uncomment when used):
#
# numpy==1.26.4
# scikit-learn==1.4.2        # TF-IDF + TruncatedSVD for the similarity layer
# rank_bm25==0.2.2           # BM25 index for hybrid retrieval
# pandas==2.2.2              # convenience for eval/ and analysis
# sentence-transformers==2.7.0   # ONLY if Phase 2 uses bge-small/e5-small instead of TF-IDF

# ---- Sandbox demo only (sandbox/app.py) ----
# rank.py imports none of this. Listed here so Streamlit Community Cloud (which installs
# from the repo-root requirements.txt) can serve the hosted demo. Stage-3 reproduction of
# the ranking step does not import streamlit. Left UNPINNED so the host resolves a streamlit
# build compatible with its Python; the app uses only basic, stable Streamlit APIs.
streamlit
